server:
  tomcat:
    remoteip:
      remote-ip-header: "x-forwarded-for"
      protocol-header: "x-forwarded-proto"
springdoc:
  swagger-ui:
    path: /swagger
spring:
  profiles:
    active: local
  config:
    import:
        - logging.yml
        - monitoring.yml